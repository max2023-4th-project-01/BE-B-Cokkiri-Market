spring:
  profiles:
    active:
      - prod
      - oauth # 로컬에서는 prod 지우고 dev로 수정
---
spring:
  config:
    activate:
      on-profile: test
---
server:
  servlet:
    encoding:
      charset: utf-8
      force: true
  port: 8080
