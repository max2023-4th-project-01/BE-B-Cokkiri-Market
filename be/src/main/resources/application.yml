spring:
  profiles:
    active:
      - dev
    include:
      - oauth
---
server:
  servlet:
    encoding:
      charset: utf-8
      force: true
  port: 8080
---
spring:
  config:
    activate:
      on-profile: test
